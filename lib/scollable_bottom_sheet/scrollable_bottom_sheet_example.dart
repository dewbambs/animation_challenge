import 'package:flutter/material.dart';

class ScrollableBottomSheetExample extends StatelessWidget {
  const ScrollableBottomSheetExample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
